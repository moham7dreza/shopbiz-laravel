<section class="col-12 col-md-{{ $col }}">
    <div class="form-group">
        <label for="{{ $id }}">{{ $label }}</label>
        <section class="{{ $class }}" id="{{ $id }}" {{ $attributes }}>{{ $text }}</section>
    </div>
</section>
